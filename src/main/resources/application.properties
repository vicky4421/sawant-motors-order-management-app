spring.application.name=sawant-motors-order-management-app

# database properties
spring.datasource.url = jdbc:mysql://localhost:3306/sawant_motors_order_management
spring.datasource.username = root
spring.datasource.password = Vivek@4421

# create table automatically
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true

# reduce loggin level
logging.level.root = warn

# turn off spring boot banner
spring.main.banner-mode = off

# changed port as app will be run as Windows service
server.port = 9000